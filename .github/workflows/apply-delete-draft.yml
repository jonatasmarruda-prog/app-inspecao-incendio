name: Aplicar exclusão de inspeção (legado desativado)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Informar status
        run: echo "Workflow legado desativado. delete-draft.js já é carregado pelo index atual e possui guard de inicialização."
