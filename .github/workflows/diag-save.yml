name: Diagnóstico do botão salvar
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Diagnóstico temporário concluído; manter apenas execução manual."
